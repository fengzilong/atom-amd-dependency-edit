'use babel';

import AmdDependencyEditView from '../lib/amd-dependency-edit-view';

describe('AmdDependencyEditView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
